aqua Scheduler

export spell


import Spell from "@fluencelabs/spell/spell_service.aqua"

import "../../aqua/main.aqua"

-- should create spell for each Member with init_args
-- will allow for precise timing
-- specific reporting
-- how? 
-- only through deployment, likely  

func spell():
    Spell "scheduler"
  --  completed <- run()
    Spell.store_log("Run completed!")